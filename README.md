# Image-Cropping-Tool-for-Datasets
This project aims to prepare images as a dataset for image processing models like ResNet. This is necessary because these models often require training with square-sized images to prevent the model from reducing the image's features or quality
